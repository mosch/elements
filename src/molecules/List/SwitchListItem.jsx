import PropTypes from 'prop-types'
import React from 'react'

export default class SwitchListItem extends React.Component {
  static propTypes = {
    value: PropTypes.string.isRequired,
    children: PropTypes.node.isRequired,
  }

  render() {
    return null
  }
}
